% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pre_si_nexp_chicago.R
\name{pre_si_nexp_chicago}
\alias{pre_si_nexp_chicago}
\title{Prepare data for single-index + exponential smoothing models (Chicago example)}
\usage{
pre_si_nexp_chicago(
  dat,
  time_col = "time",
  y_col = "death",
  gap_col = "time_gap_num",
  intercept = FALSE,
  ...
)
}
\arguments{
\item{dat}{A data frame containing the response \code{y}, covariate matrix \code{X},
and a time variable. Typically the return value of \code{\link{chicago_data}()}.}

\item{time_col}{Character; name of the time column (default: \code{"time"}).}

\item{y_col}{Character; name of the response/target column (default: \code{"death"}).}

\item{gap_col}{Character; name of the output column storing computed time gaps
(default: \code{"time_gap_num"}).}

\item{intercept}{Logical; whether to add an intercept column (a column of ones)
to the predictor matrix in expsmooth matrix(default: \code{FALSE}).}

\item{...}{Additional arguments passed to \code{\link{time_gap}()}}
}
\value{
A data frame with two components:
\itemize{
\item \code{y}: the response vector.
\item \code{xw}: a matrix (class \code{AsIs}) combining predictors,
optional intercept, and time-gap column.
}
}
\description{
This function prepares a data frame for use in the \code{si_nexp} model,
using the output of \code{\link{chicago_data}()} as input.
It extracts the response variable, computes time gaps between consecutive
observations, and constructs a combined predictor matrix that includes
the original covariates, an optional intercept, and the computed time-gap feature.
}
